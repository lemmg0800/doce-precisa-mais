// Auth middleware placeholder.
// This project uses client-side Supabase auth (see src/components/AuthProvider.tsx)
// with Row Level Security on the database. No server-side middleware is needed.
export {};
